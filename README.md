# V12.1 Atlas — fixed
Self-contained recovery build. The V12 travel data is inlined into index.html again, removing the GitHub `data/` folder as a startup dependency. All V12 GUI work remains: unified icon language, collapsible filters, dismissible details, tablet/mobile behavior, synchronized master list, events, favorites, skip, trips, Discover, seasonal and flight intelligence.
